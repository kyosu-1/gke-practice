apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd-image-updater
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://argoproj.github.io/argo-helm
    chart: argocd-image-updater
    targetRevision: 0.12.1
    helm:
      values: |
        serviceAccount:
          annotations:
            iam.gke.io/gcp-service-account: argocd-image-updater@gke-practice-kyosu.iam.gserviceaccount.com
        config:
          registries:
            - name: Google Artifact Registry
              prefix: asia-northeast1-docker.pkg.dev
              api_url: https://asia-northeast1-docker.pkg.dev
              default: true
              credentials: ext:/scripts/gcp-auth.sh
              credsexpire: 55m
        volumes:
          - name: gcp-auth
            configMap:
              name: argocd-image-updater-gcp-auth
              defaultMode: 0555
        volumeMounts:
          - name: gcp-auth
            mountPath: /scripts
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      selfHeal: true
